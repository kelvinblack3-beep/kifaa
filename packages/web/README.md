# SHARP BOYZ web (MVP)

Minimal mobile-first PWA-style demo for SHARP BOYZ Phase 1. Implements a deterministic diagnostic -> scoring -> priority -> practice loop using local storage and seeded demo questions.

Run locally:

pnpm --filter @kifaa/web dev

Open http://localhost:5173
