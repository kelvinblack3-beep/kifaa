# KIFAA-SC/software — README

This directory stores software architecture notes, deterministic math engine design, sample code, and embedded build artifacts (only when appropriate).

Recording rules:
- Do not commit large binary model files to the repo. Instead, record checksums and external storage locations or test artifacts in RESEARCH_LOG.md.
